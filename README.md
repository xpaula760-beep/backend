# Delivery Tracking Backend

Node.js + Express backend for a delivery tracking system.

## Features
- Admin authentication (JWT cookies)
- Package CRUD
- Image uploads (Cloudinary)
- Delivery status updates
- Live location support
- Secure & scalable architecture

## Setup
```bash
npm install
cp .env.example .env
npm run seed:admin
npm run dev
